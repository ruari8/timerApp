version: 1

app:
  name: timerapp
  type: static
  port: 3001

runtime:
  type: node

env:
  set:
    NODE_ENV: production

deploy:
  pre:
    - "NODE_ENV=development pnpm install"
    - "pnpm run build:web"
  post: []
